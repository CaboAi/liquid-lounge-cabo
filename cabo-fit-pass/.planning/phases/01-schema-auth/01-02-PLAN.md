---
phase: 01-schema-auth
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/unit/auth.spec.ts
  - app/auth/signin/sign-in-form.tsx
  - app/auth/reset-password/page.tsx
  - app/auth/signout/route.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]

must_haves:
  truths:
    - "Sign-in page has a Google OAuth button that calls supabase.auth.signInWithOAuth"
    - "Sign-out POST route exists at /auth/signout and redirects to /auth/signin"
    - "Reset-password page exists at /auth/reset-password and calls supabase.auth.updateUser"
    - "Middleware redirects unauthenticated users from /dashboard to /auth/signin"
    - "Middleware redirects authenticated users away from /auth/signin to /dashboard"
    - "Unit tests cover: Google OAuth button render, sign-out route, reset-password page render, middleware redirect logic"
    - "pnpm test:unit passes all auth tests"
  artifacts:
    - path: "tests/unit/auth.spec.ts"
      provides: "Unit tests for AUTH-01/03/04/05/06"
      contains: "signInWithOAuth"
    - path: "app/auth/signin/sign-in-form.tsx"
      provides: "Updated sign-in form with Google OAuth button"
      contains: "signInWithOAuth"
    - path: "app/auth/reset-password/page.tsx"
      provides: "Password reset completion page"
      contains: "updateUser"
    - path: "app/auth/signout/route.ts"
      provides: "POST route handler for sign-out"
      contains: "signOut"
  key_links:
    - from: "app/auth/signin/sign-in-form.tsx"
      to: "Supabase OAuth"
      via: "supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin + '/auth/callback' } })"
      pattern: "signInWithOAuth"
    - from: "app/auth/signout/route.ts"
      to: "/auth/signin"
      via: "supabase.auth.signOut() then redirect('/auth/signin')"
      pattern: "signOut.*redirect"
    - from: "app/auth/callback/route.ts"
      to: "app/auth/reset-password/page.tsx"
      via: "forgot-password redirectTo = /auth/reset-password; callback exchanges code then redirects"
      pattern: "reset-password"
---

<objective>
Add Google OAuth, sign-out, and password-reset-completion to the auth flow. Write
the Wave 0 unit test scaffold so all subsequent tasks have automated verify.

Purpose: AUTH-05 and AUTH-06 are completely missing; AUTH-03 is broken (no
/auth/reset-password page). Without this plan, the auth flow has no sign-out and
broken password recovery.

Output:
- tests/unit/auth.spec.ts — unit tests covering all auth behaviors (Wave 0 scaffold)
- app/auth/signin/sign-in-form.tsx — adds Google OAuth button
- app/auth/reset-password/page.tsx — new page for password reset completion
- app/auth/signout/route.ts — POST route handler that clears session
</objective>

<execution_context>
@C:/Users/mario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-auth/01-RESEARCH.md
@.planning/phases/01-schema-auth/01-VALIDATION.md
@app/auth/signin/sign-in-form.tsx
@middleware.ts
@lib/supabase/client.ts
@lib/supabase/server.ts
@tests/setup.ts
@vitest.config.ts

<interfaces>
<!-- Existing Supabase client factories — use these, do not change the files -->

From lib/supabase/client.ts:
  createClient(): SupabaseClient  // browser client — use in 'use client' components

From lib/supabase/server.ts:
  createClient(): Promise&lt;SupabaseClient&gt;  // async server client — use in route handlers

From middleware.ts (existing redirect logic):
  PROTECTED_PATHS = ['/dashboard', '/profile', '/bookings', '/settings']
  AUTH_PATHS = ['/auth/signin', '/auth/signup']
  // Unauthenticated + protected → redirect to /auth/signin?redirectTo=pathname
  // Authenticated + auth page → redirect to /dashboard
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write auth unit test scaffold (Wave 0)</name>
  <files>tests/unit/auth.spec.ts</files>
  <behavior>
    - Test: Google OAuth button renders in SignInForm with text matching /google/i
    - Test: Google OAuth button click calls supabase.auth.signInWithOAuth with provider 'google' and redirectTo containing '/auth/callback'
    - Test: POST /auth/signout calls supabase.auth.signOut() and returns redirect to /auth/signin
    - Test: Reset-password page renders a password input and submit button
    - Test: Middleware redirects unauthenticated request to /dashboard → /auth/signin?redirectTo=%2Fdashboard
    - Test: Middleware passes authenticated request to /dashboard without redirect
  </behavior>
  <action>
Create tests/unit/auth.spec.ts. Use Vitest + @testing-library/react. Mock
@/lib/supabase/client by vi.mock('@/lib/supabase/client', () => ({ createClient:
vi.fn() })) and set up the mock to return a fake supabase object before each test.

For OAuth button test: render SignInForm (import from
@/app/auth/signin/sign-in-form), find button with text matching /google/i, click
it, assert signInWithOAuth was called with { provider: 'google', options: {
redirectTo: expect.stringContaining('/auth/callback') } }.

For sign-out test: import the POST handler from @/app/auth/signout/route, call it
with a mock Request, assert signOut was called.

For reset-password page test: import from @/app/auth/reset-password/page, render
it, assert a password input and submit button are present.

For middleware tests: import the middleware function, create mock NextRequest objects
for /dashboard (no user) and /dashboard (with user), assert redirect responses.
Mock supabase.auth.getUser() return value.

Start with failing tests — implementations do not exist yet. These tests will
turn green as Tasks 2 and 3 complete.
  </action>
  <verify>
    <automated>pnpm test:unit -- --reporter=verbose 2>&amp;1 | grep -E "(PASS|FAIL|auth\.spec)"</automated>
  </verify>
  <done>
tests/unit/auth.spec.ts exists with 6+ test cases. Tests for non-existent files
(signout route, reset-password page) fail with "Cannot find module" — this is
expected RED state. Tests for existing SignInForm fail with "Google button not
found" — also expected RED. pnpm test:unit runs without crashing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Google OAuth button + sign-out route</name>
  <files>app/auth/signin/sign-in-form.tsx, app/auth/signout/route.ts</files>
  <behavior>
    - Google OAuth button renders below the email/password form with text "Continue with Google"
    - Clicking it calls supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: `${window.location.origin}/auth/callback` } })
    - POST /auth/signout calls supabase.auth.signOut() then redirects to /auth/signin
    - Sign-out does NOT redirect to any protected page
  </behavior>
  <action>
Update app/auth/signin/sign-in-form.tsx. Add a "Continue with Google" button
below the existing sign-in form (after the "Don't have an account" paragraph,
or between the form and that paragraph — separated by a divider "or"). On click:

  const handleGoogleSignIn = async () => {
    const supabase = createClient()
    await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: {
        redirectTo: `${window.location.origin}/auth/callback`,
      },
    })
  }

Style the Google button to match the existing orange design system: white background,
border border-gray-300, rounded-lg, w-full, with a simple "G" icon or text "Continue
with Google". Keep the button accessible: type="button" (not submit).

---

Create app/auth/signout/route.ts as a Next.js route handler:

  import { createClient } from '@/lib/supabase/server'
  import { redirect } from 'next/navigation'

  export async function POST() {
    const supabase = await createClient()
    await supabase.auth.signOut()
    redirect('/auth/signin')
  }

This route is called via a form with method="POST" or a client fetch. The
dashboard component (Phase 2) will add the sign-out button that calls this route.
For now the route must exist and work correctly.

NOTE: No changes needed to app/auth/callback/route.ts — it already handles OAuth
code exchange via exchangeCodeForSession and this handles both email confirmations
and OAuth callbacks.
  </action>
  <verify>
    <automated>pnpm test:unit -- --reporter=verbose 2>&amp;1 | grep -E "(auth\.spec|PASS|FAIL)"</automated>
  </verify>
  <done>
sign-in-form.tsx has a Google OAuth button. app/auth/signout/route.ts exports POST.
pnpm test:unit passes OAuth button render test and sign-out route test. pnpm build
passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add /auth/reset-password page</name>
  <files>app/auth/reset-password/page.tsx</files>
  <behavior>
    - Page renders a "New password" input and a "Update password" submit button
    - On submit, calls supabase.auth.updateUser({ password: newPassword })
    - On success, shows "Password updated — sign in with your new password" and links to /auth/signin
    - On error, shows the error message inline
  </behavior>
  <action>
Create app/auth/reset-password/page.tsx as a 'use client' component. It must:

1. Use useState for password, loading, error, success states
2. On form submit:
   - Call createClient() from @/lib/supabase/client
   - Call supabase.auth.updateUser({ password })
   - On error: setError(error.message)
   - On success: setSuccess(true) — render success message with link to /auth/signin
3. Style to match the existing auth pages (same orange gradient background, white
   card, rounded-2xl, same input/button classes)
4. Add password confirmation field (confirm password) — validate both match before
   calling updateUser; show "Passwords do not match" inline if they differ
5. Minimum password length: 8 characters (HTML minLength + client validation)

The reset link from forgot-password sends the user through /auth/callback which
exchanges the recovery code for a session, then redirects to /auth/reset-password.
By the time they land on this page they have an active session — updateUser works.
  </action>
  <verify>
    <automated>pnpm test:unit 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
app/auth/reset-password/page.tsx exists. pnpm test:unit passes all auth.spec.ts
tests (GREEN state). pnpm build passes. Visiting /auth/reset-password in browser
shows the password update form.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Google OAuth button added to sign-in page
    - POST /auth/signout route handler
    - /auth/reset-password page for completing password recovery
    - All auth unit tests passing
  </what-built>
  <how-to-verify>
    1. Run: pnpm dev
    2. Visit http://localhost:3000/auth/signin — verify "Continue with Google" button appears below the form
    3. Visit http://localhost:3000/auth/reset-password — verify new/confirm password fields and "Update password" button render
    4. Verify unauthenticated access: visit http://localhost:3000/dashboard — should redirect to /auth/signin?redirectTo=%2Fdashboard
    5. Run: pnpm test:unit — all auth.spec.ts tests must be green
    6. Optional (if Supabase credentials are configured): sign up with a real email, confirm, verify you land on /dashboard
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. pnpm test:unit passes — all tests in tests/unit/auth.spec.ts green
2. pnpm build passes — 0 TypeScript errors
3. /auth/signin shows Google button
4. /auth/reset-password renders the password update form
5. /auth/signout route exists at app/auth/signout/route.ts
6. Unauthenticated /dashboard → redirects to /auth/signin (middleware confirmed in tests)
</verification>

<success_criteria>
pnpm test:unit green. pnpm build clean. Google OAuth button in sign-in form.
Sign-out POST route created. Reset-password page renders and calls updateUser.
Middleware redirect behavior confirmed by unit tests.
</success_criteria>

<output>
After completion, create .planning/phases/01-schema-auth/01-02-SUMMARY.md
</output>
