---
plan: 03-02
phase: 3
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - supabase/migrations/006_rpc.sql
  - hooks/use-cancel-booking.ts
  - hooks/use-cancel-booking.spec.ts
  - hooks/use-transactions.ts
  - hooks/use-transactions.spec.ts
  - components/booking-card.tsx
autonomous: true
requirements: [BOOK-04, BOOK-05, BOOK-06, CRED-04]

must_haves:
  truths:
    - "cancel_booking RPC checks the 2-hour window using classes.cancellation_window_hours, sets status='cancelled', conditionally refunds credits and logs a 'refund' credit_transaction — all atomic"
    - "useCancelBooking mutation optimistically updates booking status to 'cancelled', rolls back on error, and invalidates bookings + credits caches on settle"
    - "useTransactions hook queries credit_transactions ordered by created_at DESC for a given userId"
    - "BookingCard has a Cancel button wired to useCancelBooking; shows 'Cancellation window passed' error when RPC returns cancellation_window_passed"
    - "All cancellation and transaction hook tests are green"
  artifacts:
    - path: "supabase/migrations/006_rpc.sql"
      provides: "cancel_booking PL/pgSQL function (appended via CREATE OR REPLACE)"
      contains: "cancellation_window_hours * interval"
    - path: "hooks/use-cancel-booking.ts"
      provides: "useMutation wrapping cancel_booking RPC with optimistic update"
      exports: ["useCancelBooking"]
    - path: "hooks/use-transactions.ts"
      provides: "useQuery for credit_transactions history"
      exports: ["transactionsQueryOptions", "useTransactions"]
    - path: "components/booking-card.tsx"
      provides: "BookingCard with Cancel button wired to useCancelBooking"
  key_links:
    - from: "hooks/use-cancel-booking.ts"
      to: "supabase.rpc('cancel_booking')"
      via: "createClient() inside mutationFn"
      pattern: "supabase\\.rpc\\('cancel_booking'"
    - from: "components/booking-card.tsx"
      to: "hooks/use-cancel-booking.ts"
      via: "useCancelBooking() import"
      pattern: "useCancelBooking"
    - from: "supabase/migrations/006_rpc.sql"
      to: "public.credit_transactions"
      via: "conditional INSERT inside cancel_booking for refund entries"
      pattern: "type.*refund"
---

# Plan 03-02: Cancel Booking RPC + useCancelBooking + useTransactions

## Goal

Implement the `cancel_booking` PL/pgSQL function with refund window logic, the `useCancelBooking` and `useTransactions` hooks, and wire the cancel button to `BookingCard`.

<execution_context>
@C:/Users/mario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-booking-engine-stripe/03-RESEARCH.md
@.planning/phases/03-booking-engine-stripe/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 03-01 (depends_on). Must match exactly. -->

From lib/booking-logic.ts (created in 03-01):
```typescript
export function isCancellationAllowed(
  scheduledAt: Date,
  cancellationWindowHours: number,
  now: Date
): boolean

export function isClassFull(confirmedCount: number, maxCapacity: number): boolean
export function isAlreadyBooked(bookings: Array<{class_id: string; status: string}>, classId: string): boolean
export function computeNewCredits(current: number, purchasedAmount: number): number
```

From lib/booking-errors.ts (created in 03-01):
```typescript
type BookingErrorCode =
  | 'class_not_found' | 'class_full' | 'already_booked'
  | 'insufficient_credits' | 'booking_not_found' | 'cancellation_window_passed'

const ERROR_MESSAGES: Record<BookingErrorCode, string>
```

From hooks/use-bookings.ts:
```typescript
export const bookingsQueryOptions = (userId: string) =>
  queryOptions({ queryKey: ['bookings', userId] as const, staleTime: 30_000, enabled: !!userId })
export function useBookings(userId: string): UseQueryResult
```

From lib/supabase/types.ts:
```typescript
bookings.Row: {
  id: string; user_id: string; class_id: string; status: string;
  credits_charged: number; booked_at: string; cancelled_at: string | null
}
credit_transactions.Row: {
  id: string; user_id: string; amount: number; type: string;
  reference_id: string | null; note: string | null; created_at: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0 — Write failing tests for useCancelBooking and useTransactions</name>
  <files>hooks/use-cancel-booking.ts, hooks/use-cancel-booking.spec.ts, hooks/use-transactions.ts, hooks/use-transactions.spec.ts</files>
  <behavior>
    useCancelBooking:
    - mutationFn calls supabase.rpc('cancel_booking', { p_user_id, p_booking_id })
    - when RPC returns { refunded: true }, mutation resolves with { refunded: true }
    - when RPC returns { refunded: false }, mutation resolves with { refunded: false }
    - when RPC returns { error: 'booking_not_found' }, throws with code: 'booking_not_found'
    - when RPC returns { error: 'cancellation_window_passed' }, throws with code: 'cancellation_window_passed'
    - onSettled invalidates ['bookings', userId] and ['credits', userId]

    useTransactions:
    - queryFn selects from credit_transactions where user_id = userId, ordered by created_at DESC
    - returns empty array when no transactions
    - staleTime is 30_000
    - disabled when userId is empty string
  </behavior>
  <action>
    1. Create hooks/use-cancel-booking.ts stub: export function useCancelBooking() { throw new Error('not implemented') }

    2. Create hooks/use-cancel-booking.spec.ts:
       - vi.mock('@/lib/supabase/client') — fake supabase.rpc returning controlled responses
       - describe('useCancelBooking', ...) grouping
       - Test: { refunded: true } resolves successfully
       - Test: { refunded: false } resolves (no-refund case is not an error)
       - Test: { error: 'booking_not_found' } rejects with code 'booking_not_found'
       - Test: { error: 'cancellation_window_passed' } rejects with code 'cancellation_window_passed'
       - Test: onSettled triggers invalidateQueries for bookings and credits keys

    3. Create hooks/use-transactions.ts stub: export function useTransactions(_userId: string) { throw new Error('not implemented') }

    4. Create hooks/use-transactions.spec.ts:
       - vi.mock('@/lib/supabase/client') — fake supabase.from('credit_transactions')
       - describe('useTransactions', ...) grouping
       - Test: returns array of transaction rows when data present
       - Test: returns empty array when no data
       - Test: query is disabled when userId is empty string

    5. Run pnpm test:unit -- hooks/use-cancel-booking.spec.ts hooks/use-transactions.spec.ts — MUST fail (RED).

    6. Implement useCancelBooking in hooks/use-cancel-booking.ts:
       'use client'
       import { useMutation, useQueryClient } from '@tanstack/react-query'
       import { createClient } from '@/lib/supabase/client'
       import { bookingsQueryOptions } from './use-bookings'

       type CancelVars = { userId: string; bookingId: string }
       type CancelResult = { refunded: boolean }
       type CancelError = { message: string; code: string }

       export function useCancelBooking() {
         const queryClient = useQueryClient()
         return useMutation&lt;CancelResult, CancelError, CancelVars&gt;({
           mutationFn: async ({ userId, bookingId }) => {
             const supabase = createClient()
             const { data, error } = await supabase.rpc('cancel_booking', {
               p_user_id: userId, p_booking_id: bookingId,
             })
             if (error) throw { message: error.message, code: 'rpc_error' }
             const result = data as { refunded?: boolean; error?: string }
             if (result?.error) throw { message: result.error, code: result.error }
             return { refunded: result?.refunded ?? false }
           },
           onMutate: async ({ userId, bookingId }) => {
             await queryClient.cancelQueries(bookingsQueryOptions(userId))
             const previousBookings = queryClient.getQueryData(bookingsQueryOptions(userId).queryKey)
             // Optimistically mark booking as cancelled in cache
             queryClient.setQueryData(bookingsQueryOptions(userId).queryKey, (old: unknown) => {
               if (!Array.isArray(old)) return old
               return old.map((b: { id: string }) =>
                 b.id === bookingId ? { ...b, status: 'cancelled' } : b
               )
             })
             return { previousBookings }
           },
           onError: (_err, { userId }, context) => {
             if (context?.previousBookings) {
               queryClient.setQueryData(bookingsQueryOptions(userId).queryKey, context.previousBookings)
             }
           },
           onSettled: (_data, _err, { userId }) => {
             queryClient.invalidateQueries(bookingsQueryOptions(userId))
             queryClient.invalidateQueries({ queryKey: ['credits', userId] })
           },
         })
       }

    7. Implement useTransactions in hooks/use-transactions.ts:
       'use client'
       import type { Database } from '@/lib/supabase/types'
       import { queryOptions, useQuery } from '@tanstack/react-query'
       import { createClient } from '@/lib/supabase/client'

       type CreditTransaction = Database['public']['Tables']['credit_transactions']['Row']

       export const transactionsQueryOptions = (userId: string) =>
         queryOptions({
           queryKey: ['transactions', userId] as const,
           queryFn: async () => {
             const supabase = createClient()
             const { data, error } = await supabase
               .from('credit_transactions')
               .select('*')
               .eq('user_id', userId)
               .order('created_at', { ascending: false })
             if (error) throw error
             return (data ?? []) as CreditTransaction[]
           },
           staleTime: 30_000,
           enabled: !!userId,
         })

       export function useTransactions(userId: string) {
         return useQuery(transactionsQueryOptions(userId))
       }

    8. Run pnpm test:unit -- hooks/use-cancel-booking.spec.ts hooks/use-transactions.spec.ts — MUST pass (GREEN).
  </action>
  <verify>
    <automated>pnpm test:unit -- hooks/use-cancel-booking.spec.ts hooks/use-transactions.spec.ts</automated>
  </verify>
  <done>Both hook test files green. useCancelBooking handles refund/no-refund/error branches. useTransactions queries and returns credit_transactions ordered DESC.</done>
</task>

<task type="auto">
  <name>Wave 1 — Add cancel_booking to migration + wire BookingCard cancel button</name>
  <files>supabase/migrations/006_rpc.sql, components/booking-card.tsx</files>
  <action>
    PART A — Extend 006_rpc.sql with cancel_booking function:

    Append to supabase/migrations/006_rpc.sql (after book_class) the cancel_booking function
    exactly as specified in 03-RESEARCH.md Pattern 2:
    - Signature: cancel_booking(p_user_id uuid, p_booking_id uuid) RETURNS jsonb
    - SECURITY DEFINER SET search_path = ''
    - SELECT * FROM public.bookings WHERE id = p_booking_id AND user_id = p_user_id AND status = 'confirmed' FOR UPDATE
    - If NOT FOUND: RETURN jsonb_build_object('error', 'booking_not_found')
    - Join to public.classes to get cancellation_window_hours and scheduled_at
    - v_refund := (v_class.scheduled_at - now()) >= (v_class.cancellation_window_hours * interval '1 hour')
    - UPDATE public.bookings SET status = 'cancelled', cancelled_at = now()
    - IF v_refund THEN UPDATE public.profiles credits + v_booking.credits_charged AND INSERT credit_transactions type='refund'
    - RETURN jsonb_build_object('refunded', v_refund)

    Also append to 007_rpc_grant.sql:
    REVOKE ALL ON FUNCTION public.cancel_booking(uuid, uuid) FROM PUBLIC;
    GRANT EXECUTE ON FUNCTION public.cancel_booking(uuid, uuid) TO authenticated;

    PART B — Wire BookingCard cancel button:

    Edit components/booking-card.tsx to add a Cancel button for confirmed bookings:
    - Import useCancelBooking from '@/hooks/use-cancel-booking'
    - Import ERROR_MESSAGES from '@/lib/booking-errors' (or inline the cancellation_window_passed message)
    - The component needs userId to call the mutation — update BookingCardProps to include userId: string
    - Show Cancel button only when booking.status === 'confirmed'
    - On click: call cancelBooking.mutate({ userId, bookingId: booking.id })
    - While pending: button shows 'Cancelling...' and is disabled
    - On error with code 'cancellation_window_passed': show inline error text "Cancellation window has passed. No refund will be issued."
    - On error with other codes: show error text using ERROR_MESSAGES lookup or fallback
    - On success: BookingCard re-renders with 'cancelled' status badge (cache invalidation triggers refetch)

    Keep existing BookingCardProps shape intact — just add userId field.
    Preserve existing data-testid="booked-at" attribute.
    Add data-testid="cancel-button" to the cancel Button element.
    Add data-testid="cancel-error" to the error paragraph element.

    Note: booking-card.tsx moves from a pure display component to a 'use client' component with mutation.
    Add 'use client' directive at the top.

    Note: supabase db push deferred (DNS blocker in STATE.md). SQL is the artifact.
    Test locally: supabase start && supabase db reset
  </action>
  <verify>
    <automated>pnpm test:unit && pnpm typecheck</automated>
  </verify>
  <done>006_rpc.sql contains cancel_booking function. BookingCard renders Cancel button for confirmed bookings. pnpm test:unit and pnpm typecheck pass.</done>
</task>

</tasks>

<verification>
- pnpm test:unit -- hooks/use-cancel-booking.spec.ts passes (refund/no-refund/error branches)
- pnpm test:unit -- hooks/use-transactions.spec.ts passes
- pnpm typecheck passes
- 006_rpc.sql contains cancel_booking with cancellation_window_hours interval comparison
- BookingCard has Cancel button with data-testid="cancel-button" for confirmed bookings
- BookingCard shows data-testid="cancel-error" inline error on cancellation_window_passed
</verification>

<success_criteria>
1. useCancelBooking({ userId, bookingId }) mutates; RPC { refunded: false } resolves without error
2. RPC { error: 'cancellation_window_passed' } throws with code 'cancellation_window_passed'
3. useTransactions returns credit_transactions ordered by created_at DESC
4. cancel_booking SQL: v_refund uses cancellation_window_hours * interval '1 hour' comparison
5. BookingCard shows Cancel button only for status === 'confirmed'
6. pnpm test:unit green, pnpm typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-engine-stripe/03-02-SUMMARY.md` with:
- Files created/modified
- Test results (pass count)
- Key decisions made
- Any deviations from plan
</output>
