---
plan: 03-03
phase: 3
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/007_add_credits.sql
  - lib/stripe.ts
  - app/api/stripe/create-checkout-session/route.ts
  - app/api/webhooks/stripe/route.ts
  - tests/unit/stripe-webhook.spec.ts
  - .env.example
autonomous: true
requirements: [CRED-01, CRED-02, CRED-03, CRED-05]

user_setup:
  - service: stripe
    why: "Stripe Checkout for credit pack purchases"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key (test mode: sk_test_...)"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Run: stripe listen --forward-to localhost:3000/api/webhooks/stripe — CLI prints whsec_..."
      - name: NEXT_PUBLIC_APP_URL
        source: "http://localhost:3000 for local dev; Vercel URL for production"
    dashboard_config:
      - task: "Create test-mode Credit Pack prices"
        location: "Stripe Dashboard -> Products -> Add product -> Add price (one-time, amount in cents)"
        note: "Create 3 prices matching seed plans. Copy price IDs (price_xxx) to update 004_seed.sql stripe_price_id column, OR use price_data inline (see plan task notes)"

must_haves:
  truths:
    - "POST /api/stripe/create-checkout-session authenticates user, looks up plan by planId, creates Stripe Checkout Session with metadata.userId + metadata.planId + metadata.credits, returns { url }"
    - "POST /api/webhooks/stripe reads raw body via request.text(), verifies stripe-signature header, handles checkout.session.completed by calling add_credits RPC with idempotency guard, handles invoice.paid by fetching subscription from Supabase to look up credits and calling add_credits RPC"
    - "add_credits PL/pgSQL function atomically UPDATEs profiles.credits and INSERTs credit_transactions type='purchase'"
    - "Webhook handler returns 200 immediately if credit_transactions already contains reference_id = session.id (idempotency guard prevents double-crediting)"
    - "Stripe webhook unit tests pass: mocked constructEvent, checkout.session.completed credits user, duplicate session ID is a no-op, invoice.paid calls add_credits"
  artifacts:
    - path: "lib/stripe.ts"
      provides: "Singleton Stripe client (server-only)"
      exports: ["stripe"]
    - path: "app/api/stripe/create-checkout-session/route.ts"
      provides: "POST handler returning { url } for Stripe Checkout redirect"
    - path: "app/api/webhooks/stripe/route.ts"
      provides: "POST webhook handler with raw body + signature verification + idempotency guard"
    - path: "tests/unit/stripe-webhook.spec.ts"
      provides: "Unit tests for webhook handler logic"
    - path: "supabase/migrations/007_add_credits.sql"
      provides: "add_credits PL/pgSQL function (SECURITY DEFINER, atomic UPDATE + INSERT)"
      contains: "add_credits"
  key_links:
    - from: "app/api/webhooks/stripe/route.ts"
      to: "supabase.rpc('add_credits')"
      via: "supabaseAdmin (service_role client) inside handleCheckoutCompleted and handleInvoicePaid"
      pattern: "rpc\\('add_credits'"
    - from: "app/api/stripe/create-checkout-session/route.ts"
      to: "lib/stripe.ts"
      via: "import { stripe } from '@/lib/stripe'"
      pattern: "stripe\\.checkout\\.sessions\\.create"
    - from: "app/api/webhooks/stripe/route.ts"
      to: "lib/stripe.ts"
      via: "stripe.webhooks.constructEvent(body, signature, secret)"
      pattern: "constructEvent"
---

# Plan 03-03: Stripe Checkout + Webhook + add_credits RPC

## Goal

Implement Stripe hosted checkout (redirect flow), the webhook handler with raw-body parsing and idempotency guard, and the `add_credits` PL/pgSQL function — all verified with unit tests written before implementation.

<execution_context>
@C:/Users/mario/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-booking-engine-stripe/03-RESEARCH.md

<interfaces>
<!-- Contracts the executor must use. Do not deviate. -->

From lib/supabase/types.ts:
```typescript
plans.Row: {
  id: string; name: string; credits: number; price_cents: number;
  stripe_price_id: string | null; validity_days: number; is_active: boolean; created_at: string
}
profiles.Row: {
  id: string; credits: number; /* ...other fields */ updated_at: string
}
credit_transactions.Row: {
  id: string; user_id: string; amount: number; type: string;
  reference_id: string | null; note: string | null; created_at: string
}
subscriptions.Row: {
  id: string; user_id: string; plan_id: string; status: string;
  stripe_subscription_id: string | null; created_at: string
}
```

Stripe API version pinned in this project: '2024-12-18.acacia'

Supabase client for webhook (service_role — server-only, never browser):
```typescript
import { createClient } from '@supabase/supabase-js'
import type { Database } from '@/lib/supabase/types'

const supabaseAdmin = createClient<Database>(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Wave 0 — Install stripe, create lib/stripe.ts, write failing webhook tests</name>
  <files>lib/stripe.ts, tests/unit/stripe-webhook.spec.ts, .env.example</files>
  <action>
    STEP 1 — Install stripe:
    pnpm add stripe
    Verify it appears in package.json dependencies.

    STEP 2 — Create lib/stripe.ts:
    import Stripe from 'stripe'

    export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
      apiVersion: '2024-12-18.acacia',
      typescript: true,
    })

    This file is server-only. NEVER import it in Client Components.
    Add a comment at top: // server-only — do not import in 'use client' files

    STEP 3 — Update .env.example:
    Append to .env.example:
    # Stripe (Phase 3)
    STRIPE_SECRET_KEY=sk_test_replace_me
    STRIPE_WEBHOOK_SECRET=whsec_replace_me
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    # Optional: only needed if using Stripe Elements (not needed for hosted checkout redirect)
    # NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me

    STEP 4 — Create tests/unit/stripe-webhook.spec.ts with failing tests BEFORE implementing the webhook handler.

    Mock setup:
    vi.mock('@/lib/stripe', () => ({
      stripe: { webhooks: { constructEvent: vi.fn() } },
    }))

    vi.mock('@supabase/supabase-js', () => ({
      createClient: vi.fn(() => ({
        from: vi.fn(() => ({
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockReturnThis(),
          maybeSingle: vi.fn().mockResolvedValue({ data: null, error: null }),
          single: vi.fn().mockResolvedValue({ data: null, error: null }),
        })),
        rpc: vi.fn().mockResolvedValue({ data: null, error: null }),
      })),
    }))

    Tests to write (all in describe('POST /api/webhooks/stripe', ...)):
    - Test 1: returns 400 when stripe-signature is invalid (constructEvent throws)
    - Test 2: returns 200 and calls add_credits RPC on checkout.session.completed with valid metadata
    - Test 3: returns 200 without calling add_credits when reference_id already exists in credit_transactions (idempotency guard)
    - Test 4: returns 200 and calls add_credits RPC on invoice.paid when subscription and plan are found
    - Test 5: returns 200 with { received: true } on unhandled event type

    NOTE: Tests will fail because app/api/webhooks/stripe/route.ts does not exist yet.
    Run: pnpm test:unit -- tests/unit/stripe-webhook.spec.ts — MUST fail or error (RED confirmed).
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/stripe-webhook.spec.ts</automated>
  </verify>
  <done>lib/stripe.ts exists and exports stripe client. tests/unit/stripe-webhook.spec.ts exists with 5 test cases (including invoice.paid test). .env.example updated with Stripe vars. Test run FAILS (RED phase confirmed — route does not exist yet).</done>
</task>

<task type="auto">
  <name>Wave 1 — Implement checkout session route + webhook handler + add_credits RPC</name>
  <files>app/api/stripe/create-checkout-session/route.ts, app/api/webhooks/stripe/route.ts, supabase/migrations/007_add_credits.sql</files>
  <action>
    PART A — Create app/api/stripe/create-checkout-session/route.ts:

    import { NextResponse } from 'next/server'
    import { stripe } from '@/lib/stripe'
    import { createClient } from '@/lib/supabase/server'

    export async function POST(request: Request) {
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()

      if (!user) {
        return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
      }

      const { planId } = await request.json() as { planId: string }

      const { data: plan } = await supabase
        .from('plans')
        .select('stripe_price_id, credits, name')
        .eq('id', planId)
        .eq('is_active', true)
        .single()

      // Handle missing stripe_price_id (plans table seed has NULL — use price_data inline for local dev)
      // See RESEARCH.md Pitfall 6 and Open Question 2
      if (!plan) {
        return NextResponse.json({ error: 'plan_not_found' }, { status: 404 })
      }

      const lineItems = plan.stripe_price_id
        ? [{ price: plan.stripe_price_id, quantity: 1 }]
        : [{
            price_data: {
              currency: 'usd',
              product_data: { name: plan.name },
              unit_amount: 0, // Fallback — real price must come from Stripe dashboard
            },
            quantity: 1,
          }]

      const session = await stripe.checkout.sessions.create({
        mode: 'payment',
        line_items: lineItems,
        success_url: `${process.env.NEXT_PUBLIC_APP_URL}/dashboard?payment=success`,
        cancel_url: `${process.env.NEXT_PUBLIC_APP_URL}/dashboard?payment=cancelled`,
        metadata: {
          userId: user.id,
          planId,
          credits: String(plan.credits),
        },
      })

      return NextResponse.json({ url: session.url })
    }

    PART B — Create app/api/webhooks/stripe/route.ts:

    CRITICAL WARNINGS about 03-RESEARCH.md Pattern 4:
    - Do NOT copy the supabaseAdmin.from('profiles').update() line from the research Pattern 4 code.
      That line references a non-existent increment_credits column or RPC — it is dead code and will break the handler.
    - Use ONLY supabaseAdmin.rpc('add_credits', ...) inside handleCheckoutCompleted. Never call .from('profiles').update() directly from the webhook handler.

    Correct implementation:
    - import { NextResponse } from 'next/server'
    - import { headers } from 'next/headers'
    - import { stripe } from '@/lib/stripe'
    - import { createClient } from '@supabase/supabase-js' (NOT from @/lib/supabase/server — webhook uses service_role)
    - import type { Database } from '@/lib/supabase/types'
    - import type Stripe from 'stripe'
    - supabaseAdmin created at module level with NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY
    - export async function POST(request: Request):
      - const body = await request.text() — CRITICAL: never request.json()
      - const headersList = await headers()
      - const signature = headersList.get('stripe-signature') ?? ''
      - constructEvent in try/catch → 400 on failure
      - switch on event.type:
        - 'checkout.session.completed' → await handleCheckoutCompleted(session)
        - 'invoice.paid' → await handleInvoicePaid(invoice)
      - return NextResponse.json({ received: true })

    - handleCheckoutCompleted(session: Stripe.Checkout.Session):
      - Extract userId, credits, planId from session.metadata
      - Guard: if (!userId || !credits) return
      - Idempotency: query credit_transactions WHERE reference_id = session.id AND type = 'purchase' via supabaseAdmin
      - If existing row found: return (already processed)
      - Call supabaseAdmin.rpc('add_credits', {
          p_user_id: userId,
          p_amount: parseInt(credits, 10),
          p_reference_id: session.id,
          p_note: `Stripe purchase: ${session.id}`
        })

    - handleInvoicePaid(invoice: Stripe.Invoice):
      - This is a real implementation for CRED-05 (subscription renewal credits).
      - Extract the Stripe subscription ID from invoice.subscription (string | null).
      - If no subscription ID: console.error('invoice.paid: no subscription ID, skipping') and return.
      - Look up the subscription record in Supabase:
        const { data: subscription } = await supabaseAdmin
          .from('subscriptions')
          .select('user_id, plan_id')
          .eq('stripe_subscription_id', invoice.subscription)
          .maybeSingle()
      - If subscription not found: console.error('invoice.paid: subscription not found in DB, skipping') and return.
      - Look up the plan's credits:
        const { data: plan } = await supabaseAdmin
          .from('plans')
          .select('credits')
          .eq('id', subscription.plan_id)
          .single()
      - If plan not found: console.error('invoice.paid: plan not found, skipping') and return.
      - Call supabaseAdmin.rpc('add_credits', {
          p_user_id: subscription.user_id,
          p_amount: plan.credits,
          p_reference_id: invoice.id,
          p_note: `Subscription renewal: ${invoice.id}`
        })
      - If error from rpc: console.error('invoice.paid: add_credits failed', error)

    PART C — Create supabase/migrations/007_add_credits.sql (separate file from 006_rpc.sql to avoid parallel write conflict with Plan 03-01):

    CREATE OR REPLACE FUNCTION public.add_credits(
      p_user_id uuid,
      p_amount integer,
      p_reference_id text,
      p_note text DEFAULT NULL
    ) RETURNS void
    LANGUAGE plpgsql
    SECURITY DEFINER
    SET search_path = ''
    AS $$
    BEGIN
      UPDATE public.profiles
        SET credits = credits + p_amount, updated_at = now()
        WHERE id = p_user_id;

      INSERT INTO public.credit_transactions (user_id, amount, type, reference_id, note)
        VALUES (p_user_id, p_amount, 'purchase', p_reference_id, p_note);
    END;
    $$;

    REVOKE ALL ON FUNCTION public.add_credits(uuid, integer, text, text) FROM PUBLIC;
    -- No GRANT TO authenticated — add_credits is only called by service_role via webhook

    PART D — Run tests (GREEN phase):
    pnpm test:unit -- tests/unit/stripe-webhook.spec.ts — MUST pass now.
    pnpm typecheck — MUST pass.
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/stripe-webhook.spec.ts && pnpm typecheck</automated>
  </verify>
  <done>All 5 webhook tests pass. Checkout session route returns { url }. Webhook handler verifies signature, guards against duplicates, calls add_credits RPC for both checkout.session.completed and invoice.paid. 007_add_credits.sql contains add_credits function. pnpm typecheck clean.</done>
</task>

</tasks>

<verification>
- pnpm test:unit -- tests/unit/stripe-webhook.spec.ts passes (5 tests: invalid sig 400, checkout credits user, idempotency no-op, invoice.paid calls add_credits, unhandled event 200)
- pnpm typecheck passes
- lib/stripe.ts exports stripe client with apiVersion: '2024-12-18.acacia'
- app/api/webhooks/stripe/route.ts uses request.text() (not request.json())
- app/api/webhooks/stripe/route.ts does NOT contain .from('profiles').update() — uses only rpc('add_credits')
- supabase/migrations/007_add_credits.sql contains add_credits with SECURITY DEFINER and atomic UPDATE + INSERT
- .env.example has STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, NEXT_PUBLIC_APP_URL
</verification>

<success_criteria>
1. POST /api/stripe/create-checkout-session with valid planId returns { url: 'https://checkout.stripe.com/...' }
2. POST /api/webhooks/stripe with invalid signature returns 400
3. POST /api/webhooks/stripe with checkout.session.completed + valid metadata calls add_credits RPC
4. Second delivery of same session.id is a no-op (credit_transactions check returns existing)
5. POST /api/webhooks/stripe with invoice.paid looks up subscription+plan in Supabase and calls add_credits
6. add_credits SQL function: single atomic UPDATE + INSERT in one PL/pgSQL body in 007_add_credits.sql
7. pnpm test:unit green, pnpm typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-engine-stripe/03-03-SUMMARY.md` with:
- Files created/modified
- Test results (pass count)
- Key decisions made
- Any deviations from plan (especially around stripe_price_id NULL handling)
</output>
